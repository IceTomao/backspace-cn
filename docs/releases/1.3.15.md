## Changes

- Fixed iOS standalone PWA viewport detection so the home-indicator inset is not mistaken for an open keyboard.
- Removed the extra black area below the mobile bottom navigation when the keyboard is closed.
