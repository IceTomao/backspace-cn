## Changes

- Fixed the bottom black band in iOS Home Screen web apps by placing content below the status bar.
- Fixed login on insecure local-network HTTP origins where `AudioWorkletNode` is unavailable.
- Fixed CRLF lockfile parsing in the repository's PR triage checks on Windows.
- Made vendored asset and file-permission checks reliable on Windows test runs.
