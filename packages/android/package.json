{
  "name": "@backspace/android",
  "version": "1.3.3",
  "private": true,
  "license": "AGPL-3.0-only",
  "scripts": {
    "build:web": "pnpm --filter @backspace/web build:android",
    "sync": "cap sync android",
    "build": "pnpm build:web && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
